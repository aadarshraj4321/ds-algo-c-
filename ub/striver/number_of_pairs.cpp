#include<bits/stdc++.h>




int main() {

	ios_base::sync_with_stdio(false);
	cin.tie(NULL);

	int t;
	std::cin >> t;

	while(t--) {
		int n, l, r;
		std::cin >> n >> l >> r;
		std::vector<int>arr(n);
		for(int i = 0; i < n; i++) {
			int x;std::cin >> x;
			arr.push_back(x);
		}

		for(int i = 0; i < n; i++) {

		}
	}

	return 0;
}