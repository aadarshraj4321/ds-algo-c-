#include<bits/stdc++.h>

int mostWaterTrape(std::vector<int>arr) {

	int left = 0;
	int right = arr.size() - 1;

	while(left < right) {
		int maxiSize = std::max(left, right);
		if()
	}
}

int main() {

	std::vector<int>container = {3, 0, 0, 2, 0, 4};
	std::cout << mostWaterTrape(container) << '\n';

	return 0;
}