#include<bits/stdc++.h>

std::vector<int>flipThePancake(std::vector<int>arr) {

	int n = arr.size();
	std::vector<int>newArr;
	while(n > 0) {
		int idx = findIndex(arr, n);
		
	}
}


int main() {

	std::vector<int>arr = {3,2,4,1};
	auto panCake = flipThePancake(arr);

	return 0;
}